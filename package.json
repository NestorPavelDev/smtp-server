{
  "name": "smtp-server",
  "version": "0.1.0",
  "description": "NestJS-based SMTP watcher that relays Gmail events via notification emails",
  "main": "dist/main.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/main.js",
    "start:dev": "ts-node-dev --respawn --transpile-only src/main.ts",
    "start:prod": "node dist/main.js"
  },
  "dependencies": {
    "@nestjs/common": "^10.0.0",
    "@nestjs/config": "^3.1.1",
    "@nestjs/core": "^10.0.0",
    "@nestjs/platform-express": "^10.0.0",
    "imapflow": "^1.0.168",
    "nodemailer": "^6.9.13",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.27",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.5"
  }
}
